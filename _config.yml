# Site settings
title: "Laribuck"
author: "fed"
hcprefix: "li-"
hcurl: "https://laribuck.github.io/"

url: "https://laribuck.github.io"  # http://localhost:4000 /


# encoding timezone
encoding: "utf-8"
timezone: UTC
# Build settings
baseurl: / # leave just a /
markdown: kramdown
source: .
destination: ./_site
permalink: /:slug.html
sitemap_limit: 150.0
# Default values
defaults:
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "post"

# Custom variables
version: "1.1.0"
